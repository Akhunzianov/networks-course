[package]
name = "mail_client_libs"
version = "0.1.0"
edition = "2024"

[dependencies]
lettre = { version = "0.11", features = ["smtp-transport", "native-tls", "builder"] }
clap = { version = "4", features = ["derive"] }
