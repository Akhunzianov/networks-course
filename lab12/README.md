## RIP

### А и Б

```bash
cd rip
cargo run -- [n_routers (default 4)] [extra_edges (default n/2)]
```

### В

```bash
cd rip_threads
cargo run -- [n_routers (default 4)] [extra_edges (default n/2)]
```
